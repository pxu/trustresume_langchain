{
  "_comment": [
    "LLM list prices in USD per 1,000,000 tokens, used by trustresume.telemetry",
    "to turn per-run token counts into an estimated cost.",
    "",
    "Keys are matched against the provider-reported model id: an exact match",
    "wins, otherwise the LONGEST key that is a substring of the id wins (so",
    "'gpt-4o-mini' beats 'gpt-4o' for 'gpt-4o-mini-2024-07-18'). That is why",
    "versioned/region-prefixed ids like 'global.anthropic.claude-opus-4-6-v1'",
    "do not need their own entry.",
    "",
    "A model with no entry here makes the whole run report cost_usd=null",
    "rather than a partial total (see telemetry.estimate_cost). If you want a",
    "cost figure for your provider, add its rate below.",
    "",
    "These are public list prices; VERIFY against your provider's current",
    "pricing page before quoting them. Anthropic prices are the same whether",
    "reached via Bedrock or direct. Each Claude generation needs its own row —",
    "a key ending in a digit (a version token, e.g. the '4' in 'claude-opus-4')",
    "is rejected as a match for a LATER generation's id (e.g. 'claude-opus-4-6'),",
    "so a new release reports cost_usd=null instead of silently inheriting the",
    "previous generation's rate; see telemetry._is_version_extension.",
    "Override this file's path with $TRUSTRESUME_PRICING.",
    "",
    "verified_on: 2026-08-07 (openai + anthropic entries)"
  ],
  "models": {
    "gpt-4o": { "input_per_1m": 2.5, "output_per_1m": 10.0 },
    "gpt-4o-mini": { "input_per_1m": 0.15, "output_per_1m": 0.6 },
    "claude-opus-4-6": { "input_per_1m": 15.0, "output_per_1m": 75.0 },
    "claude-sonnet-4-5": { "input_per_1m": 3.0, "output_per_1m": 15.0 },
    "claude-haiku-4-5": { "input_per_1m": 1.0, "output_per_1m": 5.0 }
  }
}
