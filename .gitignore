# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.env
.env.*
!.env.example

# --- Candidate data & secrets (never commit real user documents or credentials) ---
data/user_documents/
data/processed/*
!data/processed/.gitkeep
data/embeddings/*
!data/embeddings/.gitkeep
# Real personal résumés used for manual RAG testing (scripts/manual_rag_test.py)
# — data/sample_documents/ is otherwise synthetic-only (see data/README.md);
# these two are real PII and must never reach GitHub.
data/sample_documents/AI_Engineer_Resume.docx
data/sample_documents/Senior_SDE_Resume.pdf
chroma_data/
*.db
*.sqlite3

# --- Local LLM config overlay (may hold an API key — never commit) ---
config/llm.local.json

# --- Tests / tooling ---
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
coverage.xml
.playwright-mcp/

# --- OS / IDE ---
.DS_Store
.vscode/
.idea/
